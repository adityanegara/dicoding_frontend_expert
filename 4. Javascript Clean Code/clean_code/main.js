const createMenu = ({title, body, buttonText, cancellable}) =>{
    console.log(title);
    console.log(body);
    console.log(buttonText);
    console.log(cancellable);
}

createMenu({
    title : 'A',
    body : 'B',
    buttonText : 'C',
    cancellable : true
})